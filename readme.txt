This project is preconfigured with a Maven pom.xml file. If you have a JDK 
version compatible with Java 8 and maven installed, then you can run the 
following commands in the root directory of this project:

mvn compile
mvn exec:java 

The pom.xml should handle the rest.